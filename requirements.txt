# GUI
PyQt6>=6.6.0

# Android 自动化
uiautomator2>=3.0.0

# 定时任务
APScheduler>=3.10.0

# 节假日判断
chinesecalendar>=1.11.0

# 配置和工具
pydantic>=2.0.0
loguru>=0.7.0
requests>=2.31.0

# 图像识别（OCR 兜底 + 模板匹配）
# rapidocr_onnxruntime 会一并拉起 onnxruntime / opencv / numpy / shapely 等必需依赖
Pillow>=10.0.0
rapidocr_onnxruntime>=1.3.0
